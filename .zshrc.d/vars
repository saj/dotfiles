local less='less -CQinr'
local less_noclear='less -FQin'
local ls='ls -F'
local ls_long="${ls} -hl"

if [ -x ~/bin/mvim ]; then
    local gvisual="~/bin/mvim"
else
    local gvisual="gvim"
fi

# vim:ft=zsh
