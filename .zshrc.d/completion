autoload -U compinit
compinit

setopt AUTO_LIST
setopt LIST_PACKED

# In case of ambiguity, display a menu of matching completions, but do not add
# the first match to the input buffer.
setopt NOMENU_COMPLETE

# vim:ft=zsh
