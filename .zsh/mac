opendot() {
    local dot="$1"
    local png="$(mktemp -t opendot)"
    local newpng="${png}.png"
    mv "$png" "$newpng"
    png="$newpng"
    dot -Tpng:quartz:quartz "$dot" >"$png"
    open "$png"
    sleep 2
    rm -f "$png"
}

# vim:ft=zsh
