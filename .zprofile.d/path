__zprofile_lib_prepend_path 'PATH' "${HOME}/bin"

while read binpath; do
  __zprofile_lib_prepend_path 'PATH' "${binpath}"
done <<<$(find "${HOME}" -mindepth 1 -maxdepth 1 -type d -name 'bin.*')

# vim:ft=zsh
