#!/usr/bin/python

import ipaddr
import sys

def main(args=None):
  if not args:
    args = sys.argv

  addr = ipaddr.IPAddress(int(args[1]))
  print str(addr)

  return 0

if __name__ == '__main__':
  sys.exit(main(sys.argv))
